"\n" newline hanya melakukan enter saat diterminal, bukan di web
<br> br hanya melakukan enter saat di web, bukan diterminal
gunakan ul/ol diluar for/foreach jika menginginkan list berurutan secara langsung (1,2,3,4)
gunakan ul/ol didalam for/foreach jika menginginkan list berurutan secara terpisah
metode request get adalah metode pengiriman data melalui url dan data tsb bisa ditangkap oleh variabel superglobal get

GET POST

GET mengirimkan data melalui url
POST mengirimkan data melalui form

form bisa menggunakan get dan post
url hanya bisa menggunakan get 

kelebihan menggunakan form(POST), datanya tidak terlihat di URL. cocok untuk membuat login page


ISTILAH DATABASE
field/column = kolom
row = baris / record
key
-primary key = key unik yang membedakan data
-foreign key
auto increment = fitur untuk menambah nilai satu persatu disetiap baris / record yang masuk
relationship = hubungan antar tabel
normalization = proses untuk membuat tabel dalam database berjalan normal

MENGHUBUNGKAN MYSQL DENGAN PHP
- MYSQLi = 
- PDO (PHP DATA OBJECT)

php bisa mencetak variabel
php tidak bisa mencetak array yang tidak diberikan indeksnya
